# MongoDB Configuration (Local MongoDB Compass)
# For local development with MongoDB Compass
MONGODB_URI=mongodb://localhost:27017/chatbot

# Alternative: MongoDB Atlas (for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chatbot?retryWrites=true&w=majority

# Application Configuration
NODE_ENV=development
PORT=3000
